### 可变参数

在指定参数后面接上省略号(...)

~~~java
public static void printMax( double... numbers){ //像这里
    if (numbers.length == 0){
        System.out.println("No argument passed");
        return;
    }
    
    double result = numbers[0];
    
    //排序
    for (int i=1;i<numbers.length;i++){
        if(numbers[i] >result){
            result =numbers[i];
        }
    }
    
    System.out.println("The max value is" + result);
}
~~~

